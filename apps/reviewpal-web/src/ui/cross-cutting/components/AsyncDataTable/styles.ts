import type { SxProps } from '@mui/material';

export const asyncDataTableHeaderCellStyle: SxProps = {
  backgroundColor: 'background.paper',
};

export const asyncDataTableRowStyle: SxProps = {
  cursor: 'pointer',
};
